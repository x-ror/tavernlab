# ─── Особисті дані (не комітяться ніколи) ────────────────────────────
# Стан застосунку живе в теці даних користувача, а не тут (див. README,
# «Де лежать ваші дані»). Ці шляхи лишаються в списку, бо старіші збірки
# писали їх поруч із програмою.
tavernlab.log
tavernlab.log.*
settings.json
tiers_*.json

# ─── Вхідні дані генератора карткової таблиці ────────────────────────
# `data/standard_cards.json` і `data/wild_cards.json` — комітяться: це
# те, з чого xtask збирає `core/src/cards/table.rs`. А сирий дамп, з
# якого їх колись зібрали, — ні: 9+ МБ і легко перевантажується.
cards.json
cards_merged.json
blizzard_api_cache.json

# ─── Артефакти збірки ────────────────────────────────────────────────
target/
dist/
node_modules/

# ─── Редактори / IDE ─────────────────────────────────────────────────
.idea/
.vscode/
.ruby-lsp/
*.swp
*~
.DS_Store

# Кеш ілюстрацій (`tavernsim art-urls`). Арт — власність Blizzard:
# кешуємо для особистого використання, у репозиторій не кладемо.
art_cache/
