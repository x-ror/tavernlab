# memreader — читання пам'яті власного клієнта Hearthstone

Спайк, написаний за рішенням від 2026-08-31 (докладніше — `docs/DESIGN.md`,
розділ «Правовий режим»): живий трекер (Арена-драфт, а тепер і будь-який
бій — Ranked/Casual теж) читає стан гри напряму з пам'яті вже запущеного
клієнта замість парсингу `Power.log`/`Zone.log` із затримкою.

**Тільки читання.** Жодного запису в чужу пам'ять, жодного виконання коду
в цільовому процесі, жодної мережі. Технічно — той самий клас прийому, що
й у HDT/Firestone (HearthMirror), але сам HearthMirror приватний і його
код тут не використовується; це написано з нуля на основі відкритого коду
самого Mono-рантайму та загальнодоступного опису техніки.

## Чому це окремий крейт, а не частина `cli`

Компілюється й запускається під звичайним Linux-тулчейном (не Windows-крос-
компіляція) — процес Hearthstone тут працює під Wine, тобто це вже
нативний x86-64 Linux-процес, і `process_vm_readv` читає його пам'ять без
жодної Windows-прошарки. Тому інструмент простий: `cargo run -p memreader`.

Zero-dependency правило (CLAUDE.md) дотримано: `process_vm_readv`
викликається прямим `extern "C"` оголошенням символу glibc (в ній з 2012
року), без крейта `libc`.

## Статус: `--probe` готовий, `--deep` — ні

```
cargo run -p memreader -- --probe
```

Цей режим робить **механічну, високовпевнену** частину:
1. Знаходить PID `Hearthstone.exe`.
2. Знаходить базову адресу модуля `mono-2.0-bdwgc.dll` у `/proc/PID/maps`.
3. Парсить PE-заголовки цього файлу на диску (не пам'ять процесу) і
   знаходить RVA експортованої функції `mono_get_root_domain`.
4. Читає перші 16 байтів цієї функції з пам'яті процесу і статично
   розбирає пролог (`mov rax, [rip+disp32]; ret`), щоб дістати адресу
   глобальної змінної — вказівника на кореневий `MonoDomain`.
5. Читає цей вказівник.

Кожен крок або спрацьовує чисто, або друкує **конкретну** причину і що
з цим робити — жодних мовчазних `unwrap()`.

```
cargo run -p memreader -- --deep
```

Іде далі: пробує пройти `MonoDomain → domain_assemblies → Assembly-CSharp
→ MonoImage`. **Це поки не працює впевнено.** Офсети `MonoDomain` і
`MonoAssemblyName` не вдалося звірити з реальним вихідним кодом цієї сесії
(мережевий доступ до потрібних заголовків `mono/mono` періодично блокувався
захисним шаром самого Claude Code — не через файрвол системи і не через
дозволи проєкту, а через сесійний класифікатор безпеки, який лишається поза
моїм контролем). Докладно, які саме числа під питанням і чому —
`src/mono_layout.rs`, там кожна константа підписана рівнем впевненості.

## Як запускати разом зі мною

1. Переконайтесь, що Hearthstone запущений.
2. `cargo run -p memreader -- --probe` і скиньте мені **весь** вивід
   (включно з рядком «перші 16 байтів функції» — по ньому я перевірю, чи
   декодер прологу впізнав опкод правильно).
3. Якщо `--probe` пройшов чисто — спробуйте `--deep` і теж скиньте вивід.
   Майже напевно він зупиниться на «жодного вузла у зв'язному списку» —
   це очікувано, там і почнемо виправляти офсет `MonoDomain`.

## Права доступу

`process_vm_readv` вимагає того самого користувача (не root, не sudo) і
дозволеного `ptrace_scope`:

```
cat /proc/sys/kernel/yama/ptrace_scope
```

`0` або `1` мають працювати для процесу, запущеного тим самим користувачем.
`2` чи `3` — заблокують читання; тоді знадобиться або тимчасово знизити
`ptrace_scope` (`sudo sysctl kernel.yama.ptrace_scope=0`), або дати
`CAP_SYS_PTRACE` бінарнику.
